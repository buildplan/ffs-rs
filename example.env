##########################################
## Firefox SyncStorage RS Configuration ##
##########################################

# URL clients will use to connect to the sync server
# Examples: https://sync.example.com or https://hostname:8000
# If you use a reverse proxy (nginx, Cloudflare), use that URL here
SYNC_URL=https://sync.example.com

# Maximum number of users the server will accept
SYNC_CAPACITY=10

# MySQL/MariaDB credentials (generate strong random passwords)
# Generate with: cat /dev/urandom | base32 | head -c32
MYSQL_ROOT_PASSWORD=your_root_password_here
MYSQL_PASSWORD=your_sync_user_password_here

# Master sync encryption key (MUST be exactly 64 characters)
# Generate with: cat /dev/urandom | base32 | head -c64
SYNC_MASTER_SECRET=your_master_secret_here

# Metrics hashing secret (MUST be exactly 64 characters)
# Generate with: cat /dev/urandom | base32 | head -c64
METRICS_HASH_SECRET=your_metrics_hash_secret_here

# Log level for the sync server
# Options: error, warn, info, debug, trace
LOGLEVEL=warn
